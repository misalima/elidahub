import "./print.css";

export const metadata = {
  title: "Visualização de Impressão | ÉlidaHub",
};

export default function PrintLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
